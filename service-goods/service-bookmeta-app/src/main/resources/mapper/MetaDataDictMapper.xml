<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wanmi.sbc.goods.mapper.MetaDataDictMapper">

    <resultMap type="com.wanmi.sbc.goods.entity.MetaDataDict" id="MetaDataDictMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="key" column="key" jdbcType="VARCHAR"/>
        <result property="value" column="value" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
        <result property="delFlag" column="del_flag" jdbcType="INTEGER"/>
        <result property="descr" column="descr" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="MetaDataDictMap">
        select
          id, key, value, create_time, update_time, del_flag, descr
        from meta_data_dict
        where id = #{id}
            and del_flag = 0
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="MetaDataDictMap">
        select
          id, key, value, create_time, update_time, del_flag, descr
        from meta_data_dict
        <where>
            <if test="metaDataDict.id != null">
                and id = #{metaDataDict.id}
            </if>
            <if test="metaDataDict.key != null and metaDataDict.key != ''">
                and key = #{metaDataDict.key}
            </if>
            <if test="metaDataDict.value != null and metaDataDict.value != ''">
                and value = #{metaDataDict.value}
            </if>
            <if test="metaDataDict.createTime != null">
                and create_time = #{metaDataDict.createTime}
            </if>
            <if test="metaDataDict.updateTime != null">
                and update_time = #{metaDataDict.updateTime}
            </if>
            <if test="metaDataDict.delFlag != null">
                and del_flag = #{metaDataDict.delFlag}
            </if>
            <if test="metaDataDict.descr != null and metaDataDict.descr != ''">
                and descr = #{metaDataDict.descr}
            </if>
            and del_flag = 0
        </where>
        limit #{limitIndex}, #{limitSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from meta_data_dict
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="key != null and key != ''">
                and key = #{key}
            </if>
            <if test="value != null and value != ''">
                and value = #{value}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="updateTime != null">
                and update_time = #{updateTime}
            </if>
            <if test="delFlag != null">
                and del_flag = #{delFlag}
            </if>
            <if test="descr != null and descr != ''">
                and descr = #{descr}
            </if>
            and del_flag = 0
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into meta_data_dict(key, value, create_time, update_time, del_flag, descr)
        values (#{key}, #{value}, #{createTime}, #{updateTime}, #{delFlag}, #{descr})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into meta_data_dict(key, value, create_time, update_time, del_flag, descr)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.key}, #{entity.value}, #{entity.createTime}, #{entity.updateTime}, #{entity.delFlag}, #{entity.descr})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into meta_data_dict(key, value, create_time, update_time, del_flag, descr)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.key}, #{entity.value}, #{entity.createTime}, #{entity.updateTime}, #{entity.delFlag}, #{entity.descr})
        </foreach>
        on duplicate key update
        key = values(key),
        value = values(value),
        create_time = values(create_time),
        update_time = values(update_time),
        del_flag = values(del_flag),
        descr = values(descr)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update meta_data_dict
        <set>
            <if test="key != null and key != ''">
                key = #{key},
            </if>
            <if test="value != null and value != ''">
                value = #{value},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
            <if test="delFlag != null">
                del_flag = #{delFlag},
            </if>
            <if test="descr != null and descr != ''">
                descr = #{descr},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        update meta_data_dict set del_flag = 1 where id = #{id}
    </delete>

</mapper>

